Run Vs CMD
Run : To execuet commands with image
CMD : Once the container is created, for ex to start the node server

FROM node
WORKDIR /app
COPY . /app
RUN npm install
EXPOSE 80
CMD ["node", "server.js"]
